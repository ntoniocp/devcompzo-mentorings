import "@fontsource/montserrat";
import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  fonts: {
    heading: "Montserrat",
    body: "Montserrat",
  },
  sizes: {
    xs: "20rem",
  },
});
